import eclairLsp from "../src/eclair-lsp";

describe("Eclair LSP subprocess", () => {
  it("is possible to shutdown the Eclair process");
  it("is possible to update the VFS");
  it("should support getting hover information");
  it("can handle hover errors");
  it("should support document highlight");
  it("can handle document highlight errors");
  it("should supports getting diagnostic information");
  it("can handle diagnostic errors");
});
